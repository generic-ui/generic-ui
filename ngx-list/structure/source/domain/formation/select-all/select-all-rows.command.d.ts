import { Command } from '@generic-ui/hermes';
import { StructureId } from '../../../../core/domain/structure.id';
export declare class SelectAllRowsCommand extends Command {
    constructor(structureId: StructureId);
}
