export declare class StructureAggregationPanelConfig {
    private readonly top;
    private readonly bottom;
    constructor(top?: boolean, bottom?: boolean);
    itTopEnabled(): boolean;
    itBottomEnabled(): boolean;
}
