export declare class SchemaColumnHeader {
    private readonly top;
    private readonly bottom;
    constructor(top: boolean, bottom: boolean);
    isTopHeaderEnabled(): boolean;
    isBottomHeaderEnabled(): boolean;
}
